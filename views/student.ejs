<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end">
            <a href="/logout" class="bg-yellow-600 rounded-md px-3 py-2 inline-block">Logout </a>
        </div>
        <h3 class="text-3xl mb-5"> Hello , <%= user.name %>🙂 </h3>
        <h5 class="mb-5"> You can create new post. </h5>

        </div>
    </div>

    <div class="bg-white p-8 rounded-lg shadow-md max-w-lg w-full">
        <h1 class="text-2xl font-bold mb-6">Announcements for Standard <%= announcements.standard %>
        </h1>
    
        <% if (announcements.length> 0) { %>
            <ul class="space-y-4">
                <% announcements.forEach(announcement=> { %>
                    <li class="bg-gray-100 p-4 rounded-lg shadow-md">
                        <p class="text-gray-700">
                            <%= announcement.content %>
                        </p>
                        <p class="text-sm text-gray-500 mt-2">Posted on: <%= announcement.date.toLocaleDateString() %>
                        </p>
                    </li>
                    <% }) %>
            </ul>
        <% } else { %>
            <p class="text-red-500">No announcements available for your standard.</p>
        <% } %>
    </div>
</body>

</html>